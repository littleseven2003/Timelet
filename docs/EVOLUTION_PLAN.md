# 时屿体验升级与功能补齐计划

状态：界面及交互设计已于 2026-08-31 确认；P0/P1 基线已完成，2026-09-01 的颜色移除、快捷面板单行修复与发生频率补齐已在独立功能分支实施，等待桌面验收，P2–P4 未开始。

## 1. 目标与范围

以《时屿（Timelet）软件产品与体验设计思路》为目标体验，在已完成的 M3 成果上演进，保留 Tauri 2、Vue 3、TypeScript、Rust、本地 JSON 存储与中文优先的结构。先让时间准确、数据可靠，再完成界面与三类条目的日常操作，最后增加低频增强能力。

已确认的界面及交互以 [DESIGN.md](DESIGN.md) 第 4–9 节为准，交互定稿保存为 [UI_REFERENCE.html](UI_REFERENCE.html)。本轮按已提出的 P0/P1 范围继续实施；P2–P4 新依赖和版本调整仍需具体确认，不提前合并或发布。

不新增顶层目录，不重做正式图标，不引入大型组件库，不扩展账号、云同步、复杂日历、社交、打卡或统计系统。现有日历选择器、设计变量、置顶、备注和手动排序保留并完善；条目颜色设置按最新决定删除，类型图标、整页背景、面板入口、表单与关于布局按已确认定稿对齐。

## 2. 初次审计记录（历史基线）

下表与第 3 节保留初次问题记录，不代表本轮完成后的现状；当前实现以 DESIGN.md 5.10 和第 9 节为准。

初次检查基线：`feat/m3-polish`，提交 `fc1e3ce`；比 `main` 多 40 个提交。检查开始时工作区干净，三个工程版本字段均为 `0.1.0`，未发现版本标签。M4 双平台构建与 M5 发布准备不视为已经完成。

| 设计要求                            | 当前代码状态                                         | 处理方式                                                 |
| ----------------------------------- | ---------------------------------------------------- | -------------------------------------------------------- |
| 三类条目                            | 仅 countdown、elapsed；每日/工作日循环不等于系统提醒 | 增加独立 reminder 类型及通知规则，不自动改变旧条目类型   |
| 清透的统一视觉                      | 已有深浅色变量、类型符号、时弧预览与分区列表         | 保留基础，统一排版、状态、交互与无障碍                   |
| 快捷面板展示重点                    | 展示全部分区条目；查看全部入口错误地触发新建         | 修复入口；默认最多 6 条，可设 5–8 条，主窗口查看完整列表 |
| 新建简单、更多选项按需展开          | 预览、月历、名称、外观占据多块区域，备注和置顶仍常显 | 类型 → 标题 → 日期/时间 → 提醒开关；高级字段折叠         |
| 搜索、筛选、复制、分组              | 尚未形成对应管理能力                                 | 先搜索与基础筛选/复制，再可选分组                        |
| 完成、归档、恢复                    | 仅删除确认，无条目生命周期                           | 完成针对提醒发生记录；归档保留原内容，可恢复             |
| 提醒、稍后提醒、暂停                | 无通知插件、后台调度与送达记录                       | 独立后台调度及原生通知适配，不依赖面板是否打开           |
| 主题与设置                          | 跟随系统；仅自启和过期显示两个设置                   | 分四组组织，新增手动主题和相关默认值                     |
| 阶段/里程碑提醒、时线、指定条目数字 | 规划，尚未实现                                       | 后续增强阶段完成；只在需要的区域使用进度图形             |
| 数据导入导出、模板                  | 尚未实现                                             | 先保护迁移，再提供备份、校验、恢复与轻量模板             |

新设计中的“未来 7 天”与配图的“12 天”示例不一致；实现以真实时间和分组规则为准，不照搬示例数值。

## 3. 初次审计发现（处理结果见第 9 节）

以下结论来自代码检查或隔离的日期计算复现，不代表已完成双平台界面验收。

1. **今天的日期型倒数日被判为过期**：同一条目在分区中是“此时”，在过期过滤中却返回 true。关闭过期显示后可能隐藏今天的条目。修复自然日和时刻精度的边界。
2. **循环条目忽略开始日期**：起始日为 9 月 10 日的每日条目，在 8 月 31 日计算出 9 月 1 日为下一次发生。下一次发生不得早于开始日期。
3. **循环日期与相对时间不一致**：面板第二行使用原始日期，倒数文案使用下一次发生。统一由同一个有效发生时间生成显示。
4. **查看全部误进新建**：面板的 openMain 与 openCreate 调用相同命令。增加明确的列表入口，兼顾主窗口已在编辑的情形。
5. **初始化、时间与设置刷新不完整**：主窗口直接进入列表时未主动加载数据；面板日期标题未依赖时钟；主窗口分组缺少时间驱动；设置保存后未通知常驻面板。补齐首次加载、跨日、唤醒、重新聚焦与跨窗口刷新。
6. **未来正数日出现负数文案**：未来日期返回“已过 -10 天”的参数。保留日期，显示“尚未开始 · 还有 10 天”，到起始日自动进入持续计数。
7. **存储异常缺少保护**：读取失败回退空数组；条目保存先修改内存再写盘。区分首次使用和损坏/无权限，保留原始文件，写盘成功后再发布状态。
8. **排序规则互相覆盖**：分区、全局手动排序、原始日期与循环有效日期采用不同排序入口。明确自动和手动排序，避免拖动后跳回或置顶条目被无关分区压后。

## 4. 已确认界面与交互

本节不再维护一套平行视觉规范，统一引用 [DESIGN.md](DESIGN.md)：

| 范围                                 | 设计位置     |
| ------------------------------------ | ------------ |
| 快捷面板与唯一“打开主窗口”入口       | 4.4、5.2     |
| 主窗口、近屿摘要、分区列表与数量页脚 | 5.1、5.3–5.4 |
| 单列新建/编辑、语义预览与更多选项    | 5.5          |
| 进度、时线与时间尺度                 | 5.7–5.8      |
| 设置与独立“关于时屿”                 | 5.9          |
| 定稿参照与实现边界                   | 5.10         |
| 图标、深浅色、排版与全页背景         | 第 6 节      |
| 动效、品牌文案边界与无障碍           | 第 7–9 节    |

已确认设计覆盖原草案中的旧图标、旧面板入口、纯色背景与常驻品牌文案。开发以正式设计文档为准；下面的实施计划用于拆分依赖和验收，不再决定另一套界面方向。

## 5. 时间语义与数据规则

- 时间状态与生命周期分开：类型 countdown/elapsed/reminder；生命周期 active/archived，提醒另有发生记录的完成状态；时间状态由当前日期计算，不把临近/今天等易过时字段持久化。
- 只有未完成提醒使用“已过期”。过去的倒数日显示“已过去 N 天”，仍可查看或归档；正数日没有过期状态。旧的过期显示偏好迁移为过去日期显示偏好，不擅自删除数据。
- “未来 7 天”定义为明日起连续 7 个本地日历日；今天独立分组。计日使用日历日期差，时刻型使用时间间隔；不以固定 24 小时处理跨夏令时的换日。
- 保留原始日期/时间作为循环锚点；界面所有相对值、确切日期和排序统一使用同一下一次发生结果。当前频率明确为仅一次、每天、周一至周五；重复日期表示首次生效日，“周一至周五”不包含法定节假日和调休识别。
- 每周/月/年循环明确月末、闰日、时区和夏令时规则并配套用例。复杂规则不交给自由文本。
- 单次提醒完成后进入已完成，可撤销；重复提醒的完成只作用于本次发生，不关闭整条系列。归档/删除停止后续提醒，恢复时重新计算，不重放全部历史通知。
- 稍后提醒只改变本次有效提醒时间，保留原计划；以条目、发生时间和提醒节点生成唯一记录，持久化去重。重启或睡眠唤醒后补算，避免大量历史通知集中弹出。
- 数据格式继续使用 JSON 键 `schema_version`。本轮按已确认决定直接升至版本 2 并删除 `color`，不迁移版本 1；后续格式演进应在实施前单独确定迁移策略。读到不支持的版本、未知字段、坏文件或缺权限时停止写入并给出恢复路径，不覆盖为空文件。
- 写入成功再更新内存和广播；一次动作保持原子性，失败让两类窗口保持一致。导入先完整校验、展示变更范围，再确认应用。

## 6. 分阶段开发计划

阶段按顺序推进，各阶段拆成短期分支；每次合并前执行相关验收，不把所有变更塞进一个长期分支。

### P0：M3 收尾与可靠性基线

目标：保留已有提交，修复已发现的基础问题，建立可演进基线；不增加第三类条目或新依赖。

涉及文件：`src/utils/entries.ts`、`src/CountdownPanel.vue`、`src/ConfigWindow.vue`、`src/composables/useEntries.ts`、`src/api/settings.ts`、`src/components/SettingsSection.vue`、`src-tauri/src/{storage,settings,tray,lib}.rs`、`src/locales/`、`docs/DESIGN.md`。

步骤：

1. 在现有 M3 分支按独立修复提交处理时间边界、有效日期与查看全部入口。
2. 补共享时钟与设置变化事件，完善读写失败提示和存储事务次序。
3. 为跨日/循环/存储失败补针对性回归用例；只对高风险逻辑测试，不为样式堆叠测试。
4. 验收托盘、双窗口、拖动、自启、增删改后重启；修订现有文档过时的已实现/规划标记。
5. 达到合并门槛后以 `--no-ff` 合并到 main；如桌面验收尚未完成则保留分支，不宣称 M3 已验收。

验收：今天条目不误隐藏、未来循环不提前、跨日标题/列表/设置同步、写盘失败不污染已保存内容；M3 现有功能无回退。

### P1：界面与日常管理重构

目标：主窗口、面板、表单、设置与独立关于页对齐已确认设计；补搜索、基础筛选、复制、归档恢复、手动主题与键盘操作。暂不出现不可用的提醒按钮。

分支建议：`feat/entry-lifecycle` → `feat/workspace-layout` → `feat/quick-panel`。

涉及文件：上述前端入口、`src/style.css`、`src/types/entry.ts`、`src/api/entries.ts`、数据存储与设置；在已有 `src/components/` 拆出 `EntryList.vue`、`EntryRow.vue`、`EntryEditor.vue`、`EntryFilters.vue`，在已有 `src/composables/` 增加共享状态工具。只拆分承担多项职责的代码，不进行无关大改。

步骤：

1. 定义归档及设置字段，完成旧数据迁移/备份，再接入归档恢复与复制。
2. 抽出共用条目呈现；主窗口建立侧栏、搜索/筛选、列表及摘要，统一精修图标、深浅色背景与关于入口。
3. 重排表单和高级选项，保留日历、步进、备注、色板、预览等有效能力。
4. 面板相关条目选择、查看全部与置顶操作；主题手动切换和设置同步。
5. 补加载、无结果、错误恢复、成功反馈、删除确认、未保存提示、焦点返回与减少动态效果。

验收：0/1/6/50 个条目、长标题、深浅色、最小窗口、200% 缩放下可用；搜索命中标题/备注，归档可查找恢复；只填写核心字段能保存；键盘完成主要路径。

### P2：三类条目与基础提醒闭环

目标：完成新设计第一阶段全部核心能力：独立提醒、后台通知、完成、撤销、稍后提醒、暂停、权限与失败恢复。

分支建议：`feat/reminder-core` → `feat/desktop-notifications`。

涉及文件：`src/types/entry.ts`、条目表单/列表/面板、`src/components/EntryTypeSymbol.vue`、`src/api/entries.ts`、`src-tauri/src/storage.rs`；在现有后端目录新增 `reminders.rs`、`notifications.rs`；按批准范围修改 `Cargo.toml`、`package.json`、能力权限与 `lib.rs`。

步骤：

1. 先验证两个桌面平台的通知权限、投递和操作入口；确定新增依赖及原生适配，不把移动端接口当作桌面能力。
2. 定义提醒发生记录、完成/稍后状态和持久化去重，迁移时旧倒数日不自动启用通知。
3. Rust 后台负责调度和唤醒重算，关闭主窗口/隐藏面板仍有效；前端只展示结果和请求动作。
4. 三类条目补当天或指定时刻的基础通知；完成与稍后提醒同时支持面板和主窗口，原生通知操作遵循已验证的平台方案。
5. 测试权限关闭、发送失败、重启、时钟变化、睡眠、重复提醒和并发操作。

验收：同一次提醒不因双窗口或重启重复发送；关闭主窗口继续提醒；稍后时间重启后保留；完成可恢复；禁用通知清楚提示。完全退出应用、关机时不承诺后台送达，除非后续明确增加操作系统调度。

### P3：增强而不增重

目标：倒数日阶段提醒、正数日周年/整百天里程碑、周/月/年循环、可选分组、时线、菜单栏最近/指定条目数字。

分支建议：`feat/reminder-rules`、`feat/entry-groups`、`feat/timeline-view`、`feat/tray-display`，逐项从 main 创建、验收、合并。

涉及文件：时间计算和后台提醒模块、条目/设置模型、表单更多选项、`src-tauri/src/tray.rs`；在现有组件目录新增分组管理和 `TimelineView.vue`。

步骤：先完成循环与里程碑规则用例，再接入表单；分组作为可选字段，搜索扩展覆盖组名；时线复用列表筛选和日期结果；最后做托盘显示的平台差异与异常恢复。

验收：月末/闰日/夏令时明确，提醒节点去重；分组删除不删除条目；时线与列表一致；进度有明确起点/终点，不伪造百分比。指定条目被归档或删除时回退仅图标并解释原因。

### P4：数据能力与发布验收

目标：JSON 导入导出、迁移/备份恢复、少量模板；完善关于、使用说明及安装包验收。

分支建议：`feat/data-backup`、`feat/entry-templates`、`chore/release-readiness`。

涉及文件：存储模块、设置界面、条目表单；在现有后端目录新增 `backup.rs`；修改 `docs/DESIGN.md`、`README.md`、版本配置与打包配置，必要依赖另见第 8 节。

步骤：导出完整版本化数据；导入校验并预览冲突/重复策略，备份后合并或替换；模板仅预填常用字段，用户确认后创建；补关于与实际使用说明；完成双平台安装/升级/重启/通知验收。

验收：导出→导入后条目、分组、生命周期、提醒规则和设置一致；损坏文件不改变原数据；模板不自动启用未授权通知；安装包内图标、权限、自启与提醒正确。公网发布、签名付费或远程构建配置不含在自动执行范围内。

## 7. 版本控制与路线图调整提案

- 历史 M0–M3 保留，不重写或压缩已有提交，不在未验收时补打历史标签。
- 原计划 P0 在 `feat/m3-polish` 收尾；复查时已有 `feat/entry-lifecycle` 叠加其上，本轮保留并续接该历史。达到验收门槛后先处理既有分支合并，后续新功能从 main 切短分支；每个分支以 `git merge --no-ff` 合并。未合并的用户分支不删除；已合并分支的删除在本计划获批后按约定收尾。
- 提交遵循英文 type + 中文描述，例如 `fix(entry): 修正当天条目的过期判定`、`feat(entry): 新增归档与恢复`。存储、界面、通知、文档分别提交，保证可回溯。
- 不把里程碑号当版本号；保留当前 0.1.0 基线。建议 P1–P2 组成 **v0.2** 核心体验版本，P3 为 **v0.3** 增强版本，P4 为 **v0.4** 数据版本；这是待批准的新路线，将提醒能力从原 v0.3 提前到 v0.2。
- 现行路线中的法定节假日/调休、农历、英文启用、纯黑主题没有因本计划而删除，继续列为后续候选，不挤入新设计核心闭环。
- `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 与锁文件的版本信息同步。只在功能与安装包验收满足条件后于 main 打发布标签，不提前宣称稳定发布。

## 8. 新依赖与平台限制

P0–P1 不计划新增依赖；优先复用已有组件和运行时。

P2 候选为官方 `tauri-plugin-notification`（及需要权限界面时对应前端包）与 Rust 日历时间库 `chrono`；P4 候选为官方原生文件选择插件 `tauri-plugin-dialog`。这些仅为技术提案，尚未安装；原生通知动作适配可能需要平台专用依赖，在验证后提交明确名称、用途和权限范围，不能视为已获笼统批准。

| 能力                | macOS                                             | Windows                                                                                |
| ------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 基础通知            | 官方插件支持，仍需验证系统权限与实际包身份        | 官方文档要求安装包环境验证，开发模式表现不能代替验收                                   |
| 通知内完成/稍后按钮 | 官方插件 Actions 接口仅支持移动端；需原生方案验证 | 同左，需原生方案验证                                                                   |
| 托盘图标旁数字      | 使用菜单栏标题能力                                | Tauri 托盘标题接口不支持；建议保留图标，通过悬停与面板展示数值，并在设置中明确平台限制 |
| 面板定位            | 图标下方，验证多屏/缩放/屏幕边缘                  | 任务栏邻近可见区域，验证位置/缩放/边缘                                                 |
| 退出后的通知        | 默认不承诺；常驻后台时调度                        | 默认不承诺；常驻后台时调度                                                             |

通知原生操作不确定时，先完成原生适配验证；未经确认不能把“通知中操作”静默降级成仅面板按钮，也不能把未验证能力写为完成。Windows 数字展示的建议差异需随本计划确认。

依据（核对日期 2026-08-31）：[Tauri 通知文档](https://v2.tauri.app/plugin/notification/)、[Tauri 托盘接口](https://docs.rs/tauri/latest/tauri/tray/struct.TrayIcon.html#method.set_title)、[Chrono 文档](https://docs.rs/chrono/latest/chrono/)、[Tauri 文件对话框](https://v2.tauri.app/plugin/dialog/)。

## 9. 验证与当前结果

本轮复查起点为 `2c74c26`，实现提交为 `68e768d`（存储）、`c088cec`（日期）、`5ddec9a`（界面）、`8e23764`（失败恢复）。没有重写既有 M3 历史，没有推送、合并或打标签。

- `pnpm lint`、`pnpm build`、`pnpm test` 通过；日期回归共 6 项，覆盖上海/纽约的自然日、七天边界、夏令时、未来循环、未来正数日与面板限额。
- `cargo fmt --check`、`cargo test --offline --lib`、`cargo clippy --offline --all-targets -- -D warnings` 通过；后端回归共 6 项，覆盖旧数据、版本/日期校验、旧编辑冲突、并发写入、失败保护及重新读取。
- 已检查实际界面组件在隔离数据下的 0/1/6/50 条记录、长标题、主窗口 780×560 与最小 700×520、320×420 面板、深浅色、搜索、复制、归档恢复、删除撤销、手动排序、核心字段创建、读取失败、保存失败与草稿保护；主题与面板限额能跨窗口同步。
- macOS 调试应用包构建通过，并以独立应用身份启动；原生窗口检查连续超时，没有完成托盘/窗口实测，也没有使用用户的真实条目。
- 完整键盘路径、200% 缩放、高对比度、系统主题切换、睡眠唤醒、真实应用重启、自启与 Windows 仍未验收。不能以浏览器界面检查或临时文件重新读取代替这些项目。

详细场景、结果和复验步骤见 [本轮验收记录](VALIDATION.md)。由于桌面和双平台门槛尚未满足，分支保持未合并，版本仍为 `0.1.0`。提醒后台、完成/稍后提醒、分组/时线与导入导出未因本轮界面重构而变成已实现。

## 10. 本轮实施范围（2026-08-31）

复查基线为 `feat/entry-lifecycle` 的 `2c74c26`，工作区干净；该分支叠加于未合并的 `feat/m3-polish`。保留现有历史，在当前分支分项提交；未满足验收门槛前不合并主干、不打标签。

本轮范围沿用 P0/P1，已按以下顺序实施：

1. 存储：在现有后端目录抽出受锁保护的文件事务，读取失败与未知版本禁止覆盖，备份失败中止写入；补并发、损坏、失败与重启读取用例。涉及 storage.rs、settings.rs 及共用持久化模块。
2. 时间：统一自然日计算与传入时钟，修正夏令时、循环锚点、未来七天边界、有效日期排序；面板按条目数量选取、排除归档与摘要重复。涉及 utils/entries.ts 及针对性用例。
3. 界面：共用背景、条目行、近屿摘要与时钟/设置状态；补单列编辑、错误反馈、未保存保护和键盘操作。保留原日期选择器、备注、展示单位和自启。
4. 验证与记录：执行类型/构建、静态检查及业务用例，使用隔离数据检查界面；同步设计与实施状态，明确桌面、重启及 Windows 未验收项。

复查发现：静态检查通过并不代表 P0/P1 完成。现有写盘事务释放锁过早、坏文件被当空数据、按固定 24 小时换日、面板限额按分组数计算且未排除归档；近屿在主列表重复、表单缺少未保存/失败保护。以上属于本轮修复范围。

不新增依赖，不操作用户现有数据或系统自启项，不改写历史或推送发布。P2 的后台提醒与原生通知适配、P3/P4 的增强与导入导出另行推进。

## 11. 发生频率与面板修正（2026-09-01）

本轮从 `feat/entry-lifecycle` 切出 `feat/recurrence-and-panel-polish`，不引入依赖、不调整顶层目录、不合并主干或打标签。按独立职责完成：

1. 删除前端与 Rust 存储中的条目颜色字段、默认值、色板、取色控件、详情展示和测试样本；数据结构升至版本 2，不保留旧颜色数据兼容。
2. 快捷面板的条目与近屿摘要保持单行，核心时间完整显示，标题和元信息在窄空间省略并提供完整提示；主窗口保持自然换行。
3. 带时刻倒数日将发生频率提升到核心表单，提供仅一次、每天、周一至周五；重复日期作为首次生效日，预览和列表显示“下次”有效日期与时刻。
4. 回归覆盖每天 17:00 的目标前、目标时刻、目标后推进，单次到期，周五推进到周一，未来首次日期、夏令时和后端无效频率组合。

代码提交依次为 `3b780c9`、`dcc6df1`、`431cbb9`。详细检查结果见 [VALIDATION.md](VALIDATION.md)；完成原生桌面复验前保留在功能分支。

## 12. 应用图标显隐与合并复验（2026-09-03）

目标：在通用设置提供 macOS 隐藏 Dock / Windows 隐藏任务栏按钮，保持托盘入口、现有主窗口操作及重启恢复。默认不隐藏，不增加依赖或顶层目录。

涉及文件：设置 API、设置状态与页面、双语文案、Rust 设置存储与窗口启动逻辑，以及设计和验收记录。执行顺序：

1. 新增 `hideAppIcon` 设置、默认值及两端原生显隐逻辑；Windows 的后续新建窗口使用同一设置。
2. 将系统切换和写盘串行执行，保存失败恢复原图标策略，并覆盖损坏文件、系统失败、保存失败及重读用例。
3. 使用独立标识和示例数据进行原生回归；实测发现共用开关在保存失败后外观未恢复，修复受控输入行为。
4. 同步设计与验收记录，完成余下桌面验收后按原定方式合并；验证中断时保留分支，不宣称全部通过。

分支 `feat/dock-visibility` 续接 `fix/featured-layout` 的未合并历史，未改写已有提交。新设置实现提交为 `70eee5b`。已验证部分 macOS 原生交互、编辑与归档后重启，余下复验因桌面锁定中断；详细证据及未测项见 [VALIDATION.md](VALIDATION.md)。

续测已完成开关失败显示修复（`66c9586`）的原生回归、正常保存恢复、隔离自启开关与失败回滚、删除及近屿选择后重启、最小窗口尺寸恢复和真实每日目标推进。另修复“现在”向下取整到五分钟的问题，与已确认的逐分钟精度一致。剩余托盘点击、系统变化等未测项未被自动勾选；测试图标位置不在当前屏幕内，随后桌面再次锁定，因此仍保留分支，不合并、不打标签。
